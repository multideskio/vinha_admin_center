# CI/CD Pipeline - Desabilitado temporariamente devido a limitações de billing
# Para reativar, descomente as linhas abaixo e remova este comentário

# name: 🔄 CI/CD Pipeline
# 
# on:
#   push:
#     branches: [ main, dev ]
#   pull_request:
#     branches: [ main, dev ]

jobs:
  test:
    name: 🧪 Test & Build
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🟢 Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v6
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: 📦 Install dependencies
      run: npm ci

    - name: 🔍 Run ESLint
      run: npm run lint

    - name: 🎨 Check Prettier formatting
      run: npm run format -- --check

    - name: 🔧 TypeScript type check
      run: npm run typecheck

    - name: 🏗️ Build project
      run: npm run build

    - name: 🧪 Run tests
      run: npm test

  security:
    name: 🔒 Security Scan
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '18'
        cache: 'npm'

    - name: 📦 Install dependencies
      run: npm ci

    - name: 🔍 Run security audit
      run: npm audit --audit-level=high

    # CodeQL Analysis (GitHub Advanced Security required for private repos)
    # - name: 🛡️ Run CodeQL Analysis
    #   uses: github/codeql-action/init@v3
    #   with:
    #     languages: javascript
    # 
    # - name: 🔍 Perform CodeQL Analysis
    #   uses: github/codeql-action/analyze@v3